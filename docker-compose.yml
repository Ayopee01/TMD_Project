version: "3.8"

services:
  test2-app:
    container_name: test2-app
    build: .
    restart: always
    ports:
      - "3005:3000"  # ✅ host:3005 -> container:3000
    env_file:
      - .env
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
