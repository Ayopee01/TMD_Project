services:
  app:
    container_name: tmd_project-app
    build: .
    restart: always
    ports:
      - "3005:3000"   # ✅ host:container (3005:3000)
    env_file:
      - .env
    networks:
      - my-network

networks:
  my-network:
    driver: bridge   # ✅ ใช้ network เดิมร่วมกัน
