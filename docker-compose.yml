version: '3.8'

services:
  app:
    container_name: mtoken_n-app
    build: .
    restart: always
    ports:
      - "3005:3000"
    env_file:
      - .env
    # ✅ จุดสำคัญ: ยัดเข้า Network เดียวกัน
    networks:
      - my-network

# ✅ จุดสำคัญ: สร้าง Network กลางให้มันคุยกัน
networks:
  my-network:
    driver: bridge
